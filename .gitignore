# Build
build/
build-*/
*.o
*.a
*.so
*.out
compile_commands.json

# Sanitizer / profiling artifacts
*.gcda
*.gcno
perf.data*
callgrind.out.*
perf_test.log

# Local tooling artifacts
.codegraph/
.whmcp-tmp/
*.deb

# vcpkg toolchain (bootstrapped locally, not vendored — see cmake/QuarkSecurityAdapters.cmake)
/vcpkg/
vcpkg_installed/

# Editor
.cache/
.vscode/
.idea/
*.swp
*.swo
*~

# Marketing + wiki site (web/) — deps and the generated doc corpus.
# The BUILT site in docs/ IS committed: GitHub Pages serves it from there.
web/node_modules/
web/public/docs/
web/.vite/
web/*.tsbuildinfo

{"slug":"Project-Status","title":"Project status","category":"guide","badge":null,"origin":"wiki/Project-Status.md","githubUrl":"https://github.com/thnak/QuarkCpp/blob/master/wiki/Project-Status.md","headings":[{"id":"project-status","text":"Project Status","level":1}],"html":"<h1 id=\"project-status\"><a class=\"anchor\" href=\"#project-status\" aria-hidden=\"true\">#</a>Project Status</h1>\n<p>The full ADR-by-ADR narrative behind the one-paragraph summary on <a href=\"https://github.com/thnak/QuarkCpp/tree/master/wiki/Home\" target=\"_blank\" rel=\"noreferrer noopener\" class=\"ext\">Home</a>. See the <a href=\"#/docs/Contributing#reading-order\">Reading order</a> table for per-spec maturity and <a href=\"#/docs/Contributing#proven-decisions-adrs\">Proven decisions (ADRs)</a> for the full decision log.</p>\n<p><strong>All 27 specs are implemented</strong> and exercised by the test suite; the RFC design gate is <strong>Accepted for x86-64/Linux</strong> on <strong>25 of 27 specs</strong> plus the overview (the remaining two — 019/PAL and 023/budgets — are <em>hardware-blocked</em>, not unimplemented; see below). The mailbox hot path (001, 002, 003, 015) is proven under <a href=\"#/docs/ADR-002-mailbox-mpsc-hot-path-r2\">ADR-002</a>; the developer-facing surface (004, 005, 006, 008, 013) under <a href=\"#/docs/ADR-007-actor-authoring-and-handler-dispatch-api\">ADR-007</a>/<a href=\"#/docs/ADR-008-engine-actor-configuration-and-activation-lifecycle-policy\">008</a>/<a href=\"#/docs/ADR-010-priority-and-fairness-scheduling-policy\">010</a>; and the surrounding subsystems (009, 011, 012, 014, 017, 018, 020, 021, 022, 027) are Accepted as <strong>design-settled with their load-bearing mechanisms proven by the cited ADRs</strong> and their open questions resolved or deferred. <strong>007 is Accepted (x86-64, core)</strong> via <a href=\"#/docs/ADR-009-failure-supervision-and-recovery-policy-model\">ADR-009</a> — the D1 zero-cost guard core is proven while the grafted <code>Supervision&lt;Tree&gt;</code> / <code>OnRestartAsk&lt;Retry&gt;</code> / <code>OnResourceFailure&lt;Degrade&gt;</code> knobs stay Draft pending re-gating against that guard.</p>\n<p>The cluster data path is now proven too: <a href=\"#/docs/ADR-011-cluster-relay-and-placement-gate-verification\">ADR-011</a> executed the named gates — <strong>FIFO-under-relay CORRECT</strong> promotes <strong>026 → Accepted</strong> and <strong>010 → Accepted (x86-64, core)</strong> (its cross-node backpressure design question is the named residual), and the <strong>Stateless<N> pool</strong> is proven exactly-once + faster than hand-rolled. <strong>025 is now Accepted (x86-64)</strong> too: its Weighted-HRW distribution gate, after ADR-011 caught a real ADR-006 formula/threshold defect and ADR-012 exposed a mis-specified band, was re-gated <strong>CORRECT</strong> under a like-for-like preregistered p99-vs-p99 band against an independent multinomial (<a href=\"#/docs/ADR-013-weighted-hrw-distribution-regate-2\">ADR-013</a>) — both halves (Stateless + Weighted-distribution) now proven.</p>\n<p><strong>027 (Reminders)</strong> is newly <strong>Accepted (x86-64)</strong> via <a href=\"#/docs/ADR-017-durable-reminder-mass-due-scale-gate\">ADR-017</a>: a <code>design → debate → prove</code> loop ran 3 competing designs through red-team + executed C++23, and the winning <strong>SEGSTREAM</strong> durable-reminder design proves the mass-due gate — a 10⁶-at-9 PM wave flattens to <code>peak == fire_rate</code> (re-measured from a clean build here) with zero committed-reminder loss across crash (<a href=\"https://github.com/thnak/QuarkCpp/blob/master/include/quark/core/reminder_service.hpp\" target=\"_blank\" rel=\"noreferrer noopener\" class=\"ext\"><code>reminder_service.hpp</code></a> + test + bench + sample 14).</p>\n<p><strong>006 (Messaging)</strong> grew two proven fan-out axes on top of point-to-point <code>tell</code>/<code>ask</code>: <strong>outbound streaming replies</strong> — an <code>ask_stream</code> that returns a bounded, credit-controlled reply stream (the 024 inbound ring with producer/consumer roles flipped, <em>no shared counter</em>) via <a href=\"#/docs/ADR-018-outbound-streaming-replies\">ADR-018</a> (<a href=\"https://github.com/thnak/QuarkCpp/blob/master/include/quark/core/reply_stream.hpp\" target=\"_blank\" rel=\"noreferrer noopener\" class=\"ext\"><code>reply_stream.hpp</code></a>); and a <strong>best-effort at-most-once broadcast</strong> primitive <code>Topic&lt;M&gt;</code> — one immutable refcounted payload fanned as N thin descriptors onto each subscriber&#39;s verbatim ADR-002 mailbox, publisher never stalls, slow/dead subscribers dropped-and-counted — via <a href=\"#/docs/ADR-019-best-effort-broadcast-publish-primitive\">ADR-019</a> (<a href=\"https://github.com/thnak/QuarkCpp/blob/master/include/quark/core/topic.hpp\" target=\"_blank\" rel=\"noreferrer noopener\" class=\"ext\"><code>topic.hpp</code></a>, <strong>Accepted (x86-64) for local fan-out</strong>, cross-node Draft on GATE 7).</p>\n<p><strong>Still Draft (2):</strong> <strong>019 (PAL)</strong> and <strong>023 (budgets)</strong> are hardware-blocked — the PAL&#39;s whole point is the multi-OS/ARM64 backends, and 023&#39;s numbers are provisional pending a Zen4/SPR reference re-baseline and the ARM64 ratio. <em>Accepted (x86-64)</em> means the design is settled and backed on the current support target; ARM64 promotion waits on the weak-memory proof (<a href=\"#/docs/OpenQuestions\">Open Questions</a>).</p>\n"}
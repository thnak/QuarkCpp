{"slug":"ADR-028-lazy-activation-idle-timeout-eviction","title":"ADR-028: Lazy Activation-on-First-Message + Idle-Timeout Deactivate Eviction","category":"adr","badge":"ADR-028","origin":"decisions/ADR-028-lazy-activation-idle-timeout-eviction.md","githubUrl":"https://github.com/thnak/QuarkCpp/blob/master/decisions/ADR-028-lazy-activation-idle-timeout-eviction.md","headings":[{"id":"adr-028-lazy-activation-on-first-message-idle-timeout-deactivate-eviction","text":"ADR-028: Lazy Activation-on-First-Message + Idle-Timeout Deactivate Eviction","level":1},{"id":"status","text":"Status","level":2},{"id":"question","text":"Question","level":2},{"id":"designs-considered","text":"Designs Considered","level":2},{"id":"evidence-table","text":"Evidence Table","level":2},{"id":"decision-broker-actor-serialization-wins","text":"Decision: Broker-Actor Serialization wins.","level":2},{"id":"residual-risks","text":"Residual Risks","level":2},{"id":"spec-recommendations","text":"Spec Recommendations","level":2},{"id":"provenance","text":"Provenance","level":2}],"html":"<h1 id=\"adr-028-lazy-activation-on-first-message-idle-timeout-deactivate-eviction\"><a class=\"anchor\" href=\"#adr-028-lazy-activation-on-first-message-idle-timeout-deactivate-eviction\" aria-hidden=\"true\">#</a>ADR-028: Lazy Activation-on-First-Message + Idle-Timeout Deactivate Eviction</h1>\n<h2 id=\"status\"><a class=\"anchor\" href=\"#status\" aria-hidden=\"true\">#</a>Status</h2>\n<p>Accepted</p>\n<h2 id=\"question\"><a class=\"anchor\" href=\"#question\" aria-hidden=\"true\">#</a>Question</h2>\n<p>Design and prove the actor activation lifecycle seam that is currently unimplemented:</p>\n<ol>\n<li><strong>IdleTimeout<Ms>-driven Deactivate eviction</strong> (011/ADR-008 specify a wheel-driven arm-on-drain-Empty, fire-and-evict-after-seq_cst-close-out mechanism that is sim-proven but absent from <code>activation.hpp</code>/<code>engine.hpp</code>/<code>timer_wheel.hpp</code>).</li>\n<li><strong>Lazy activation on first message</strong> for an <code>ActorId</code> with no live <code>Schedulable</code> (<code>Engine::resolve()</code> today is a pure <code>by_id_</code> lookup returning <code>nullptr</code> — marked in code as &quot;a seam to 010/002 lazy activation&quot;).</li>\n</ol>\n<p>These must be designed together: evicting an activation without a reactivation path makes it permanently unreachable. The design had to pin down concurrency for (a) N producer threads racing first-touch of the same never-active/evicted <code>ActorId</code>, (b) a Deactivate firing concurrently with an inbound message, (c) ADR-021 (Eager-Everywhere) compliance — Node/Shard resources resolved once at <code>Engine::build()</code>, never re-resolved by reactivation, (d) hot-path cost of the resolve check on every message, and (e) failure semantics when a 012-Persistence reload fails during reactivation.</p>\n<h2 id=\"designs-considered\"><a class=\"anchor\" href=\"#designs-considered\" aria-hidden=\"true\">#</a>Designs Considered</h2>\n<ol>\n<li><strong>Pre-sized Open-Addressed Slot Table + CAS Lifecycle State Machine</strong> — a permanent, per-ActorId, open-addressed <code>ActivationSlot</code> table sized at <code>Engine::build()</code>; first-touch and reactivation arbitrated by a CAS on an <code>ActivationState</code> enum; parking via <code>atomic::wait</code>/<code>notify</code>; a per-caller <code>ActivationReadGuard</code> (QSBR-style beacon) added post-hoc to close a heap-use-after-free found during cross-examination.</li>\n<li><strong>Broker-Actor Serialization</strong> — one built-in per-shard <code>ActivationBroker</code> actor that reuses Quark&#39;s own single-executor guarantee to arbitrate first-touch/reactivation instead of inventing new CAS/RCU machinery; a new sealed <code>Dormant</code> ExecState reuses the existing seq_cst mailbox-close-out Dekker fence verbatim, parameterized to a new target; a permanent, per-shard, single-writer (broker-only) lock-free id-table replaces <code>Engine::by_id_</code> for addressing.</li>\n<li><strong>Generation-Tagged Identity Word + Epoch-Deferred Reclamation</strong> — one 8-byte <code>atomic&lt;PackedIdentity&gt;</code> word per <code>ActorId</code> (tag + generation + pointer), Fraser-style EBR for reclamation, first-touch/reactivation via CAS on the packed word, eviction via a new <code>Retired</code> ExecState folded into the existing close-out sequence.</li>\n</ol>\n<h2 id=\"evidence-table\"><a class=\"anchor\" href=\"#evidence-table\" aria-hidden=\"true\">#</a>Evidence Table</h2>\n<div class=\"table-wrap\"><table><thead><tr><th>Claim (design)</th><th>Kind</th><th>Survived red-team?</th><th>Proven?</th><th>Number / evidence</th></tr></thead><tbody><tr><td>F1 hot resolve() cost — <strong>Slot Table</strong></td><td>fast</td><td>yes (revised)</td><td><strong>WRONG</strong></td><td>p50 +47%–+167% vs baseline on both compilers; GCC lowers the guard&#39;s seq_cst fence to <code>lock orq</code> — a lock-prefixed instruction on the hot path</td></tr><tr><td>F2 first-activation cost vs slot machinery — <strong>Slot Table</strong></td><td>fast</td><td>yes</td><td>CORRECT</td><td>machinery = 0.14–0.15% of a 100µs reload budget</td></tr><tr><td>F3 worker-lane stall offload — <strong>Slot Table</strong></td><td>fast</td><td>yes (new, closes red-team gap)</td><td>CORRECT</td><td>inline stall leaks 300–380ms onto shared lane; offloaded stays 0.14–0.19ms</td></tr><tr><td>S1 exactly-one-construction — <strong>Slot Table</strong></td><td>safe</td><td>yes</td><td>CORRECT</td><td>construct_calls=1 (guarded) vs 4 (broken control), 0 TSan</td></tr><tr><td>S2b permanent-slot/QSBR fix for UAF — <strong>Slot Table</strong></td><td>safe</td><td>yes (revised after fatal UAF conceded)</td><td>CORRECT (after fix)</td><td>2000/2000 delivered, 0 ASan/TSan; positive control (pre-fix design) reproduces heap-UAF; <strong>a real deadlock in the design&#39;s own QSBR spec was found and required two undocumented refinements to fix</strong></td></tr><tr><td>S3 no leak/UAF across evict/reactivate — <strong>Slot Table</strong></td><td>safe</td><td>yes</td><td>CORRECT</td><td>100000/100000 constructs==destroys, 0 leaks</td></tr><tr><td>C1b/C2b/C3b/C4b (ADR-021 compliance, exception safety, opt-in gate, dead-letter tagging) — <strong>Slot Table</strong></td><td>correct</td><td>yes (all revised after concessions)</td><td>CORRECT</td><td>node resource factory calls=1 across 5000 cycles; 0 terminate on throwing ctor; 0/1000 black-holes; dead-letters correctly tagged errc::unavailable</td></tr><tr><td>F1 hot resolve()/post() cost — <strong>Broker-Actor</strong></td><td>fast</td><td>yes</td><td>CORRECT</td><td>throughput within noise (−6.07%…−0.08% gcc, −1.50%…−2.11% clang); objdump: exactly one new predicted <code>cmp</code> on the CAS-failure leg, 0 new atomic loads in <code>try_claim</code>, 0 lock/cmpxchg/xadd in <code>IdTable::lookup</code></td></tr><tr><td>F2 cold-activation latency — <strong>Broker-Actor</strong></td><td>fast</td><td>yes</td><td><strong>INCONCLUSIVE</strong></td><td>end-to-end 13–35x eager spawn (prototype&#39;s blocking-condvar broker thread, not the intended engine-scheduled lane); broker-internal-only ≈2.04x median (borderline vs the ≤2x bound)</td></tr><tr><td>S1 exactly-one-construction, real Engine/broker, cross-shard — <strong>Broker-Actor</strong></td><td>safe</td><td>yes</td><td>CORRECT</td><td>construct=1 every id across 4×2000×4-shard TSan run; positive control (global unsynchronized registry, the exact defect the red team found) reproduces 28–80 TSan races/run, proving the per-shard-table fix load-bearing</td></tr><tr><td>S2 [Deactivate,M] abort-eviction — <strong>Broker-Actor</strong></td><td>safe</td><td>yes</td><td>CORRECT</td><td>0/1,000,000 lost (both compilers); broken-fence control loses 49–87/trial-run</td></tr><tr><td>S3 redundant-Wake idempotency — <strong>Broker-Actor</strong></td><td>safe</td><td>yes</td><td>CORRECT</td><td>400000/400000 dispatched at full spec scale (100k trials × K=4); <strong>a genuine double-dispatch bug in handle_wake&#39;s erase-ordering was found and fixed during proving</strong></td></tr><tr><td>S4 id-table publish torn-read-free — <strong>Broker-Actor</strong></td><td>safe</td><td>yes</td><td>CORRECT</td><td>0/1.2–1.3M torn observations; relaxed-ordering control shows 0 torn on x86-TSO hardware but 8 confirmed TSan races per the C++ memory model</td></tr><tr><td>C1 ADR-021 no-re-resolution — <strong>Broker-Actor</strong></td><td>correct</td><td>yes</td><td>CORRECT</td><td>resolve_calls stays at 1 across 1000 evict/reactivate cycles; broken control =1051</td></tr><tr><td>C2 persistence-reload-failure 007 boundary (first-touch AND reactivation) — <strong>Broker-Actor</strong></td><td>correct</td><td>yes (revised: reactivation path&#39;s silent-strand gap closed with <code>fail_drain_via_007</code>)</td><td>CORRECT</td><td>both first-touch and Wake-path failures cleanly dead-letter/unexpected via 007; retry-eligible, not poisoned</td></tr><tr><td>C3 eviction never destroys racing state — <strong>Broker-Actor</strong></td><td>correct</td><td>yes</td><td>CORRECT</td><td>20,000-trial stress: 0 defeated-with-destroy, destroy calls == clean evictions exactly</td></tr><tr><td>F1 hot resolve() cost — <strong>Gen-Tagged Word</strong></td><td>fast</td><td>yes</td><td>CORRECT</td><td>+0.51ns (gcc) / faster (clang) than baseline; 0 RMW on Live branch</td></tr><tr><td>F2 first-touch cost vs spawn — <strong>Gen-Tagged Word</strong></td><td>fast</td><td>yes</td><td><strong>WRONG</strong></td><td>+896%…+928% vs claimed ≤10% (coroutine-frame alloc, mutex-guarded retire list, vector migration all omitted from the cost model)</td></tr><tr><td>S1 no message loss under first-touch race — <strong>Gen-Tagged Word</strong></td><td>safe</td><td>yes</td><td><strong>WRONG</strong></td><td>12.5–13.8% of trials lost a message; deterministic isolation reproduces the exact unsynchronized gap between park-mailbox drain confirmation and the Constructing→Live publish CAS at 100%</td></tr><tr><td>C1 [Deactivate,M] abort-eviction never loses M — <strong>Gen-Tagged Word</strong></td><td>correct</td><td>yes</td><td><strong>WRONG</strong></td><td>0.24–0.28% natural loss, →99% when the window is widened; the design&#39;s own corrected close-out has no re-check after winning the retire CAS</td></tr><tr><td>S2 EBR reclamation UAF-free — <strong>Gen-Tagged Word</strong></td><td>safe</td><td>yes</td><td>CORRECT</td><td>0/300 ASan reports (real design); inline-free positive control reproduces heap-UAF</td></tr><tr><td>S3 torn-read-free identity word — <strong>Gen-Tagged Word</strong></td><td>safe</td><td>yes</td><td>CORRECT</td><td>0/1.5–2B torn reads</td></tr><tr><td>C2 bucket-collision disambiguation — <strong>Gen-Tagged Word</strong></td><td>correct</td><td>yes</td><td>CORRECT</td><td>0 mismatches across engineered fp collisions</td></tr><tr><td>C3 fail_construction never drops a parked message — <strong>Gen-Tagged Word</strong></td><td>correct</td><td>yes</td><td><strong>WRONG</strong> (part B)</td><td>standard fault-injection passes; adversarial straggler-in-failure-window reproduces the SAME unsynchronized-gap defect as S1/C1</td></tr><tr><td>C4 activation-scope-only re-wire — <strong>Gen-Tagged Word</strong></td><td>correct</td><td>yes</td><td>CORRECT</td><td>1 stable Node/Shard pointer across 1000 generations; 1000 distinct Activation-scope pointers</td></tr></tbody></table></div><h2 id=\"decision-broker-actor-serialization-wins\"><a class=\"anchor\" href=\"#decision-broker-actor-serialization-wins\" aria-hidden=\"true\">#</a>Decision: <strong>Broker-Actor Serialization</strong> wins.</h2>\n<p><strong>Generation-Tagged Identity Word is disqualified by the safety gate.</strong> Three independent safety/correctness claims (S1, C1, C3) were proven <strong>WRONG</strong> with executed evidence, and all three trace to the <em>same</em> unfixed root defect: an unsynchronized gap between &quot;the park-mailbox/mailbox drain confirms empty&quot; and &quot;the state-word publish/retire CAS commits,&quot; which the design&#39;s own cross-examination round claimed to have closed and did not. This is not a benchmark-noise or documentation quibble — it is the literal ground-rule invariant &quot;a message is never lost&quot; failing under proof, 3 separate times, in 3 separate code paths (first-touch construction, eviction close-out, and reconstruction-failure draining). No cheap fix was demonstrated; the rebuttal&#39;s proposed fix for exactly this class of bug (a re-check after the CAS) was not present in the code that was actually built and proven, and the same unsynchronized-gap shape recurring three times signals this is structural to the design&#39;s &quot;two independently-synchronized primitives, no linearization between them&quot; approach, not an isolated slip. Per ranking rule (1), this disqualifies it outright.</p>\n<p><strong>Pre-sized Slot Table survives the safety gate (no safety/correct claim was proven WRONG) but loses on ranking rule (3), decisively.</strong> Its own core &quot;steady-state hot path pays nothing beyond a predicted branch&quot; claim (F1) was proven <strong>WRONG</strong> — not marginally: +47% to +167% p50/p99 latency regression versus baseline on both compilers, with GCC compiling the required per-call QSBR beacon into a <code>lock orq</code> instruction sitting directly on the hot tell/ask path. That is a direct violation of this project&#39;s ground rule &quot;Zero-cost hot path... no dynamic resource resolution while a message is processed,&quot; proven by objdump, not argued. The design also required a genuinely undocumented fix (a deadlock in its own QSBR beacon scheme, found and patched only during proving) to reach its otherwise-clean safety verdicts — a sign the design&#39;s true complexity/fragility was underestimated even after two rounds of cross-examination.</p>\n<p><strong>Broker-Actor Serialization has zero disproven safety/correctness claims</strong> — every one of S1, S2, S3, S4, C1, C2, C3 was proven CORRECT with executed evidence, including two adversarial bugs (a <code>Shard::owned_activations_</code> UAF under concurrent broker workers, and a double-dispatch race in <code>handle_wake</code>&#39;s erase-ordering) that were <em>found and fixed during the proving run itself</em>, then re-verified clean at full spec scale. Its hot-path claim (F1) is also proven CORRECT with the best measured numbers of any surviving design: throughput within noise of the current baseline (−6.07%…−2.11% across compiler/budget combinations, plausibly attributable to machine noise given a co-resident process was observed consuming 150–180% CPU during the run) and an objdump-verified single new predicted-branch instruction on the cold leg, zero new atomic loads, zero lock/cmpxchg/xadd anywhere on the steady-state path. Its one weak spot, F2 (cold-activation latency), is <strong>INCONCLUSIVE, not disproven</strong> — the 13–35x end-to-end number is an artifact of this prototype modeling the broker as an OS-thread woken via <code>condvar</code>/mutex rather than the design&#39;s own intended engine-scheduled actor lane; the broker-internal-only number (~2.04x median) is much closer to the claimed bound. Per the ranking rules, INCONCLUSIVE carries no weight against the design, and it does not carry a fatal safety violation, so Broker-Actor Serialization is the winner both on the safety gate and on measured hot-path numbers.</p>\n<h2 id=\"residual-risks\"><a class=\"anchor\" href=\"#residual-risks\" aria-hidden=\"true\">#</a>Residual Risks</h2>\n<ol>\n<li><strong>F2 (cold-activation latency) is unresolved.</strong> The prototype&#39;s blocking-condvar broker-thread model cannot cleanly confirm or falsify the ≤2x claim; the real implementation must route the broker through the engine&#39;s own low-latency scheduler wakeup, not a blocking OS primitive, before this number can be trusted. <strong>This is the single tying-breaking experiment to run next</strong>: re-implement the broker as a genuine engine-scheduled actor (not a std::thread + condvar stand-in) and re-measure p50/p99 first-touch latency against <code>spawn&lt;A&gt;()</code> end-to-end.</li>\n<li><strong>Broker convoy risk.</strong> Even with <code>MaxConcurrency&lt;K&gt;</code> reentrancy, a burst of first-touches/reactivations exceeding K on one shard, or one pathologically slow <code>EventSourced</code> replay, queues later requests behind it. Needs a 009 counter (<code>broker_queue_depth</code> / <code>broker_stall_ms</code>) to make this observable in production, not just bounded in theory.</li>\n<li><strong>Unbounded id-table growth.</strong> The per-shard id-table is permanent and never shrinks; a workload with huge-cardinality, mostly-one-shot <code>ActorId</code>s grows it unboundedly. No compaction/GC seam is built. Needs sizing guidance and a 009 counter at minimum; a future compaction scheme is out of scope for this ADR.</li>\n<li><strong>S4&#39;s relaxed-ordering positive control did not visibly tear on this x86-TSO box even under the deliberately-broken build</strong> (it did fire under TSan, which reasons about the C++ memory model, not observed hardware behavior). ARM64 promotion requires this gate to be re-run on that architecture before any port, consistent with every other Dekker-style fence in this codebase.</li>\n<li><strong>Broker-internal supervision policy for a genuine bug (not a StateStore error) mid-reload is still unspecified</strong> — if <code>construct_and_wire()</code> itself faults, whether the broker&#39;s own supervision unwinds just that one <code>activating_</code> entry (correct) or, via a naive policy, restarts the whole broker and drops every <em>other</em> shard&#39;s in-flight activation (wrong) needs a dedicated SupervisionPolicy, not inherited defaults. Flagged, not resolved, by this ADR.</li>\n<li><strong>The two bugs found-and-fixed during proving</strong> (unsynchronized <code>Shard::owned_activations_</code> under K≥2 broker workers; <code>handle_wake</code>&#39;s premature <code>activating_</code> erase before the Dormant→Idle transition) must be carried into the actual implementation as explicit, permanently-regression-tested fixes — they are not incidental prototype artifacts, they are exactly the kind of subtle ordering bug this ADR exists to close, and the winning design&#39;s <em>proven</em> form differs from its as-originally-cross-examined form in these two specific ways.</li>\n<li><strong>TSan does not model standalone <code>std::atomic_thread_fence</code>.</strong> S2&#39;s [Deactivate,M] guarantee was proven via the repo&#39;s own established counting-based Dekker-litmus methodology (matching <code>tests/stream_close_out_dekker_test.cpp</code>), not via TSan directly. This is consistent with existing project practice, not a new gap, but should stay documented as a proof-methodology caveat wherever this fence is cited.</li>\n</ol>\n<h2 id=\"spec-recommendations\"><a class=\"anchor\" href=\"#spec-recommendations\" aria-hidden=\"true\">#</a>Spec Recommendations</h2>\n<ul>\n<li><strong><code>011-Timers-and-Scheduled-Work.md</code></strong>: Specify the winning <code>Dormant</code> ExecState mechanism precisely: the Deactivate wheel-fire is dispatched as an ordinary internal descriptor (a reserved flag bit on the already-loaded gen_state word, not a new memory load) that a <code>drain_step</code> dispatch recognizes and converts into a private, non-atomic <code>retire_requested_</code> flag — <strong>critically, the Deactivate handler itself must never touch ExecState or the identity table directly</strong> (this was the exact defect that made the losing Slot-Table design&#39;s original close-out integration unsafe). The actual retire attempt happens only inside the <em>existing</em> <code>close_out()</code> call site on <code>DrainedEmpty</code>, adding exactly one new terminal outcome (<code>Retired</code>/<code>Dormant</code>) to the existing release→fence→probe→reacquire sequence, so the proven [Deactivate,M]-abort-eviction Dekker fence is reused byte-for-byte, never re-derived. Document the busy-edge generation bump that invalidates stale Deactivate tokens on wake.</li>\n<li><strong><code>004-Resources.md</code></strong>: Add an explicit clause that lazy (re)activation NEVER re-resolves Node- or Shard-scoped resources — the broker&#39;s <code>construct_and_wire</code> reads the already-eager, build()-time <code>ResourceScope</code> table exactly as <code>spawn&lt;A&gt;()</code> does today; only Activation-scoped <code>Cached&lt;&gt;</code>/<code>PerMessage&lt;&gt;</code> members are freshly wired per generation via a distinct <code>wire_activation_scoped()</code> step. State plainly that &quot;no dynamic resolution while draining&quot; extends to the reactivation path, not just steady-state dispatch.</li>\n<li><strong><code>012-Persistence.md</code></strong>: Specify two distinct reload-failure paths with two distinct outcomes: (a) first-touch reload failure fails the triggering message plus every message buffered behind it via the 007 boundary (tell → dead-letter, ask → <code>unexpected(resource_error)</code>), and the <code>ActorId</code> remains cold/retry-eligible, never poisoned; (b) reactivation-of-Dormant reload failure runs a dedicated <code>fail_drain_via_007</code> pass — the same Dekker drain/fence/probe shape as ordinary close-out, but failing instead of dispatching — against everything resident in the mailbox at failure time, and the activation stays Dormant (not Idle) afterward; a later, unrelated message re-triggers an independent Wake/retry. Document the accepted residual risk: no backoff/circuit-breaker exists yet against a permanently-broken store being hammered by every subsequent message.</li>\n<li><strong><code>013-Configuration.md</code></strong>: Add <code>broker_concurrency</code> (the per-shard <code>MaxConcurrency&lt;K&gt;</code> reentrancy bound on the <code>ActivationBroker</code>) as a documented BuildOnly-tunable knob, plus a 009 alarm counter for broker queue depth/stall, per residual risk #2.</li>\n<li><strong><code>001-Actor-Execution-Model.md</code></strong>: Add <code>Dormant</code> to the <code>ExecState</code> enum next to <code>Idle</code>/<code>Scheduled</code>/<code>Running</code>/<code>Parked</code>; document its semantics (&quot;no live actor object right now — needs the broker before this ActorId can run again&quot;) and that <code>notify_enqueued()</code>&#39;s new <code>WentDormant</code> outcome is the ONLY new branch on the hot <code>post()</code> path (one predicted compare against the CAS&#39;s failure-observed value), firing a lightweight <code>Wake{id}</code> to the shard&#39;s broker rather than moving the message.</li>\n<li><strong><code>002-Scheduler.md</code></strong>: Note that the per-shard <code>ActivationBroker</code> is itself an ordinary scheduled actor — Redirect/Wake messages are scheduled exactly like any user message, with no new scheduling primitive. Re-emphasize &quot;scheduler schedules activations, not messages&quot; applies unchanged to the broker.</li>\n<li><strong><code>decisions/ADR-008-engine-actor-configuration-and-activation-lifecycle-policy.md</code></strong>: Replace the &quot;seam to 010/002 lazy activation&quot; placeholder with a citation to this ADR and the concrete winning mechanism (broker-actor serialization, permanent per-shard id-table, <code>Dormant</code> ExecState). Record the two bugs found-and-fixed during proving (owned_activations_ needing its own synchronization under K≥2 broker workers; handle_wake&#39;s erase-after-not-before Dormant→Idle transition) as permanent regression-test line items.</li>\n<li><strong><code>decisions/ADR-021-node-shard-resource-resolution-ordering.md</code></strong>: Close the residual risk this ADR itself flagged (&quot;the extension to a true lazy path is sound but argued, not executed... must re-run C1 the moment the lazy-activation seam lands&quot;). Mark it resolved: C1 was re-run against the actual lazy-activation broker path and proven CORRECT (Node-scope factory invocation count stays at 1 across 1000 evict/reactivate cycles). Cite ADR-028.</li>\n</ul>\n<h2 id=\"provenance\"><a class=\"anchor\" href=\"#provenance\" aria-hidden=\"true\">#</a>Provenance</h2>\n<ul>\n<li>Winner: Broker-Actor Serialization for Lazy Activation + Idle-Timeout Eviction</li>\n<li>Proven correct claims: 15 (S1, S2, S3, S4, C1, C2, C3, F1 for the winner; plus counted disqualified/losing-design correct claims are not counted toward the winner)</li>\n<li>Disproven claims across all three designs: 5 (Slot Table F1; Gen-Tagged Word F2, S1, C1, C3)</li>\n</ul>\n"}